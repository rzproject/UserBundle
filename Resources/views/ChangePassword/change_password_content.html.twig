{{ form_start(form, {'action': path('sonata_user_change_password'), 'attr': {'role': 'form', 'novalidate':'novalidate'}   }) }}
<div class="box-body">
	{{ form_widget(form) }}
</div>
<div class="box-footer">
	<button type="submit" class="btn btn-danger pull-right">{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</button>
</div>
{{ form_end(form) }}
